apiVersion: v1
kind: Service
metadata:
  name: forex-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: forex-backend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
